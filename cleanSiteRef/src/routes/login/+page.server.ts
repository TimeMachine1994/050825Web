//form actoins go here

// i'm going ot have  adifferent form action for each action the user can do on the login page.
// see, we can do reset password, register, and login. 


import type { Actions } from './$types'

export const actions = {
    login: async (event) => {

    },
    resetPassword: async (event) => {
        
    },
    register: async (event) => {
        
    }
}